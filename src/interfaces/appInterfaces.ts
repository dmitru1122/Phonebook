export interface IContanct {
  id: string;
  phoneNumber: string;
  email: string;
  firstName: string;
  lastName: string;
}

export interface IMyButton {
  actionTitle: string;
  myStyle: string;
}
